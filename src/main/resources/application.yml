hyperon:
  database:
    url: jdbc:h2:./db/hyperon.demo.dictionary;AUTO_SERVER=TRUE;MVCC=TRUE;TRACE_LEVEL_FILE=FALSE;IFEXISTS=TRUE
    username: sa
    password: sa
    dialect: h2
  dev:
    mode: false # indicates whether to take into account unpublished session
    user: admin # user whose session will be taken into account if dev.mode is set to true

server.port: 8082


